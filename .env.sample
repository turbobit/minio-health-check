# MinIO Health Monitor 환경 변수 설정
# 이 파일을 .env.local로 복사하고 실제 값으로 수정하세요

# ===========================================
# 알림 설정 (선택사항)
# ===========================================

# Slack 알림
# Slack Workspace에서 Incoming Webhook을 생성하고 URL을 입력하세요
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK/URL

# Mattermost 알림
# Mattermost에서 Incoming Webhook을 생성하고 URL을 입력하세요
MATTERMOST_WEBHOOK_URL=https://your-mattermost.com/hooks/YOUR_WEBHOOK_ID
# 특정 채널에 알림을 보내려면 채널명을 입력하세요 (선택사항)
MATTERMOST_CHANNEL=your-channel-name

# 이메일 알림 (현재는 콘솔 로그만 출력)
# 실제 이메일 전송을 위해서는 SendGrid, AWS SES 등을 연동해야 합니다
EMAIL_TO=your-email@example.com

# ===========================================
# 보안 설정 (선택사항)
# ===========================================

# Vercel Cron Job 보안
# Vercel Cron이 호출할 때 인증에 사용할 비밀키
# 설정하지 않으면 인증 없이 접근 가능합니다
CRON_SECRET=your-secret-key-here

# ===========================================
# 개발 환경 설정
# ===========================================

# Next.js 환경
NODE_ENV=development

# ===========================================
# 사용 예시
# ===========================================

# 1. 이 파일을 .env.local로 복사
# cp env.sample .env.local

# 2. 실제 값으로 수정
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXXXXXX
# MATTERMOST_WEBHOOK_URL=https://mattermost.yourcompany.com/hooks/xxxxxxxxxxxxxxxxxxxxxxxxxx
# MATTERMOST_CHANNEL=alerts
# CRON_SECRET=my-super-secret-key-12345

# 3. Vercel 배포 시 환경 변수 설정
# - Vercel 대시보드 → 프로젝트 → Settings → Environment Variables
# - 위의 변수들을 Production, Preview, Development 환경에 추가
